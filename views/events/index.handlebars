<h2>Upcoming Events</h2>
<ul>
  {{#each events}}
    <li>
      <a href="/events/{{this._id}}">
        <strong>{{this.title}}</strong>
      </a> — {{this.eventDate}} at {{this.startTime}}<br>
      {{this.address}}
    </li>
  {{/each}}
</ul> 